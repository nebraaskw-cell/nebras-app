def generate_guidance(prompt):
    """
    Lightweight placeholder guidance response.
    Keeps the API contract stable until external AI providers are integrated.
    """
    return {
        "response": f"مساعد نبراس تلقى طلبك: {prompt}",
        "provider": "stub",
    }

